# EI 3.0 原版 800×600 UI 完成审计

更新时间：2026-08-10  
第一证据：`/home/tetsuya/NAS/TMP/EI传奇3.0客户端/Mir3.exe`、`mir3.dat`、`Data/*.wil`、`Data/*.wix`

这份审计不把“已经有 JSON”当成“已经还原完成”。每个条目都区分：

- **已闭合**：原版二进制/资源已经给出可复现的资源、位置、尺寸或绘制调用。
- **候选可视化**：可以在 800×600 预览中画出，但仍有运行时语义、父窗口原点或透明边界未闭合。
- **待闭合**：不能用现代 Zircon 坐标或人工拖动代替，必须继续从原版调用链、运行时对象或资源交叉验证。

## 范围与统一产物

| 要求 | 当前证据/产物 | 状态 |
|---|---|---|
| 固定 800×600 视口 | `layout.json.viewport`、`Tools/wilviewer.py /ui` | 已闭合 |
| 原版资源与 Frame | `resource-family-catalog.json`、各窗口专项 JSON、WIL/WIX 分析 | 已闭合到资源级 |
| 统一布局/绘制记录 | `docs/research/ei-ui-layout/layout.json`，当前 28 条窗口/按钮记录 | 已闭合到当前覆盖范围 |
| 证据等级与 pending 边界 | 各专项 JSON、`verify_mir3_ui_evidence.py` | 已闭合 |
| 原版绘制层级 | `draw-order-evidence.json`、`window-traversal-evidence.json` | 候选；运行时可见链仍会变化 |
| 可视化装配预览 | `Tools/wilviewer.py` | 已有；需继续补专项内容 |
| 坐标差异叠加 | `/ui` 本地截图上传、透明度调节、状态保存 | 已有验证入口 |
| UI 状态记忆 | `localStorage` 与 HUD modal hash 状态 | 已有 |

## 界面覆盖矩阵

| 界面 | 已恢复的原版事实 | 当前缺口 |
|---|---|---|
| 主 HUD/底部操作栏 | Frame 50、HUD 原点 `(0,465)`、动态血球/魔法/经验绘制链、按钮帧与命中区 | 运行时血量/魔法/经验注入和部分文字颜色 |
| 小地图/地图 | MMap/FMMap 选择、地图 ID 映射、小地图目标 `(672,0)-(800,128)`、地图专用合成链 | 外部/烘焙边框、标记语义、运行时地图状态 |
| 人物状态/装备 | Frame 200、11 条构造 Rect、属性全局字段、装备资源选择器模式与绘制链 | 装备槽人类名称、最终资源上下文与部分颜色语义 |
| 背包 | Frame 250、6×6/36px 命中网格、Frame 94 详情路径、负重文字格式、物品记录字段 | 主数值/打包字段对应现代业务字段，Frame 94 最终业务用途 |
| 技能窗口 | Frame 400、Magic.exp 读取链、11 组 410–465 控件帧对、15px 列表行距、0x104 记录步长 | Magic.exp 运行时解码字段与类别/图标业务映射 |
| 任务 | Frame 700、列表 19 行/15px、详情 Frame 705、721–724 控件事件到消息 0x418 | 0x418 的业务名称、正文分隔/颜色 |
| 聊天 | Frame 350、历史区/输入区 Rect、19 行、视觉 14px、六种频道/拒聊命令、滚动控件 | 共享文字渲染器的字体/颜色编号、节点字段语义 |
| NPC 对话 | Frame 1100/1101/1102、18px 条带节奏、token 解析、16 项上限、14/21px 行距、动态控件重排 | 字形最终绘制调用、动态目标字段语义、按钮业务名 |
| 商店/仓库 | Frame 1000–1003、状态 0–4、控件状态门控、选中记录前置条件 | 状态到买卖/仓库/扩展业务名称，运行时协议字段 |
| 交换 | 双区结构、6×5 网格候选、窗口/资源调用链 | 交易状态转换和控件业务名称 |
| 组队 | Frame 900、成员两列/20px、允许/拒绝状态文字、五个控件 | 成员记录字段/图标顺序、打开状态 |
| 行会 | Frame 600、滚动列表、18 行上限、三状态绘制、9 个控件及像素中文标签 | 每个标签和点击分支的精确绑定、特殊标记颜色 |
| 系统设置 | Frame 750、四组选项、Config.ini 键、两个音量滑块 | 音频引擎最终消费路径 |
| 坐骑 | Frame 850、四个按钮韩文标签、状态字节门控、原版命令字面量 | 状态字节枚举及额外覆盖层 |
| 确认/提示框 | Frame 950、`-1/-1` 居中规则、三按钮命中 Rect、原版支付/丢金币/仓库等消息 | 两个构造簇的状态关系、运行时 hover/click |
| 公告/全图 ID15 | Frame 602、固定 `(107,110)`/`584×252`、状态文字与控件、显示隐藏分派 | 1024 宽资源的裁剪关系、独立公告还是行会状态 |
| 好友/社交 | 已做静态负范围审计；无独立好友窗口构造器 | 继续追动态对话/Interface1c 控件入口，不凭空新增面板 |

## 还原器必须遵守的规则

1. `SetRect`、`PtInRect`、WIL 头部尺寸和实际绘制调用优先于现代 C# Zircon 坐标。
2. 构造器的原始 `x/y`、窗口对象的最终原点、窗口相对控件位置必须分开保存。
3. 资源 Frame 的尺寸不自动等于窗口容器尺寸；透明像素边界也不能自动变成命中区。
4. `candidate`/`pending` 内容在预览中必须显示候选标识，不能伪装成原版确定值。
5. 截图差异只能验证/发现问题，不能反向手工改写一级坐标。
6. 原版客户端文件只读；新增结论进入 `docs/research/ei-ui-layout/` 并更新 `RESEARCH_LOG.md`。

## 每轮完成门槛

```text
python3 Tools/enrich_mir3_layout_evidence.py
python3 Tools/verify_mir3_ui_evidence.py
python3 -m py_compile Tools/wilviewer.py
git diff --check
```

只有专项 JSON 的 pending 项减少、统一 `layout.json` 同步、预览模式可访问且提交已推送，才算一轮有效进展。当前审计证明项目仍在进行中，不得调用 goal complete。
