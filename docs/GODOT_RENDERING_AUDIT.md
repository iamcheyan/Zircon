# Godot 渲染审计与修复清单

更新时间：2026-08-08

## 目标

让 Godot 客户端的地图、玩家、怪物、NPC、物品和特效达到原版 Mir 客户端的绘制语义：同一套坐标、基线、行排序、阴影、染色、混合、光照和 UI/标签流程。

## 已确认的问题

| 编号 | 项目 | 原版行为 | 当前 Godot 状态 | 优先级 |
|---|---|---|---|---|
| R01 | 玩家阴影 | `DrawShadow2` 斜切人物边界并以半透明黑色绘制 | 有资源时使用真实 Shadow；无资源时使用稳定椭圆 fallback | P0 |
| R02 | 怪物/NPC 阴影 | 使用 ZL Shadow 通道，透明度约 0.5 | 已接入资源 Shadow，缺资源时椭圆 fallback | P0 |
| R03 | 物品落地表现 | 居中、名称、焦点框、稀有度发光 | 已有居中、悬停名称和稀有度光效，离线审计已触发真实 Ground.Zl 物品 | P1 |
| R04 | Overlay 染色 | 护甲、武器、盾牌、头盔使用 Overlay 通道 | 已接入 ZL Overlay，当前主要恢复护甲 | P0 |
| R05 | ZL Shadow/Overlay 数据 | 按 Shadow/Overlay 尺寸与偏移解码 | 已接入旧格式和 ZL2 payload | P0 |
| R06 | 坐骑 | 马体、马影、特殊马匹 Overlay | 已接入马体、马影和暗/皇族效果库 | P1 |
| R07 | 玩家外观特效 | 翅膀、光环、武器/护甲外观、混合绘制 | 已按旧端补齐主要翅膀、光环、戒指、武器和四类法器；外观统一使用普通 Image 透明 | P1 |
| R08 | 对象标签 | 名称、聊天、毒状态、伤害数字、目标描边 | 已接入名称、聊天、毒标记、伤害数字和目标框 | P1 |
| R09 | 对象光源 | 玩家、怪物、法术和特效的逐对象光源 | 玩家/怪物 Stat.Light 与 MirEffect FrameLight 已接入 | P1 |
| R10 | 怪物特殊绘制 | 多层怪物、特殊高度、附加特效和 Blend | 已覆盖三层 Lobster、四类附加特效和大型对象基线；附加外观使用普通 Image，技能特效才使用黑色透明键 | P1 |
| R11 | 地图遮挡 | 地形对象、对象、角色统一按 RenderY 排序 | MapTerrainRow 与对象审计均确认使用 `100 + CellY` | P0 |
| R12 | 过滤与缩放 | 2x 像素缩放保持原图清晰 | 已启用 nearest canvas texture filter | P1 |

## 修复顺序

1. 建立统一 `SpriteDraw` 绘制入口，集中处理主图、阴影、Overlay、偏移、透明度和缩放。
2. 修复 ZL 的主图/Shadow/Overlay 数据读取，并为玩家、怪物、NPC、物品补齐阴影。
3. 恢复玩家装备染色、坐骑及坐骑特殊效果。
4. 恢复物品稀有度光效、名称和焦点框。
5. 恢复对象名称、聊天、毒状态、伤害数字和目标描边。
6. 接入对象光源、特效光源和昼夜颜色。
7. 补齐怪物特殊分层、Blend 和大型对象基线。
8. 用 headless 测试、编译和游戏截图逐项验收，并在本表更新状态。

## 验收标准

- 人物、NPC、怪物脚下可见且位置正确的阴影；人物移动时阴影随基线移动。
- 装备染色与原版角色界面颜色一致，Overlay 不改变透明轮廓。
- 物品不再像悬浮图标，焦点框和稀有度光效跟随物品格坐标。
- 角色、对象和特效不会穿过应当遮挡它们的建筑/地形。
- 夜晚、黄昏、雨雪和局部光源能够同时生效。
- `dotnet build GodotClient/ZirconClient.csproj --no-restore` 无错误。
- MapTest/headless 渲染测试通过，且运行时无连续图片解码错误。

## 当前状态

- [x] 审计结果已记录
- [x] 统一绘制辅助层
- [x] 阴影和 Overlay
- [x] 坐骑与外观特效
- [x] 物品/NPC/怪物标签与反馈
- [x] 对象光源
- [x] 怪物特殊分支与 Blend（常见分支）
- [x] 刺客动作 ArmourShift
- [x] 玩家走路/跑步/骑马动作与服务端 Distance 同步
- [x] 普通攻击、职业/武器攻击帧、法术、远程攻击回包
- [x] 受击、推开、死亡动作收尾
- [x] 编译与 MapTest/headless 验收
- [ ] 桌面像素截图验收（当前容器无 X11/Wayland display）

## 本轮实现记录

- `ZlReader` 现在支持主图、Shadow、Overlay 的旧格式和 ZL2 payload，并缓存三类纹理。
- 修复对象包默认透明 `Colour` 被误当作染色的问题；透明色现在统一回退为不透明白色，避免“名称/血条可见但怪物身体不可见”。
- `ObjectRenderer` 和 `PlayerRenderer` 已加入资源阴影、无资源椭圆 fallback、名称、毒标记、聊天气泡、伤害数字、坐骑和常见外观特效。
- `GameScene` 已接入玩家/怪物 `Stat.Light`，光照层会绘制对象光源；地面稀有物品光效继续使用 `ProgUse.Zl`。
- 2x 世界缩放下默认纹理过滤切换为 nearest，避免像素素材被线性插值软化。
- 编译结果：0 errors；MapTest：350x350 地图、背景 100、中层 388、前景 400，渲染完成。
- `--render-audit` 已用真实数据库对象 `Guard` 与 `Hexa Holy Stone` 触发 Shadow、标签和 RenderY 绘制；headless/dummy 模式无运行时错误。
- `--render-audit` 进一步覆盖 `M-Hum.Zl` 玩家和 `Ground.Zl` Gold 物品，四类对象首帧诊断均正常。
- 2026-08-08：实际执行 `godot-mono --headless --path GodotClient/ --scene Scenes/MapTestScene.tscn -- --render-audit`；地图 350x350，背景 100、中层 388、前景 400，真实 Monster/NPC/Player/Item 均完成首帧诊断，无图片解码异常。
- 2026-08-08：桌面截图首次暴露 MapTest 自有图库路径解析失效（统计有地图格但画面灰底）；已改为复用 `LibraryCache`。复测截图可见地形、树木、岩石、玩家、Monster、NPC、物品和阴影，避免测试场景假通过。
- 2026-08-08：桌面 `--render-audit` 截图已实际保存并检查；`--action-audit` 24 个动作序列全部 PASS（走路、跑步、骑马、攻击、施法、蓄力、受击、采集、钓鱼、驯服、隐身、推开、死亡等）。
- 特效 `FrameLight` 已纳入对象光照层，魔法爆炸测试特效带有局部暖色光源。
- 2026-08-08：透明通道按资源语义拆分：玩家/坐骑/怪物附加外观和天气使用普通 Image 或天气专用透明处理；真正的技能序列帧继续使用 `GetEffectTexture` 的黑色透明键，避免黑色衣服、武器、翅膀和怪物细节被误抠除。
- 2026-08-08：按旧端 `ExteriorEffectManager` 补齐 FlameAura2、紫色触手、钻石火焰、凤凰、冰王、蓝蝶翅膀及四类 Thurible 双层帧；方向相关外观恢复前后层规则和坐骑偏移。
- 2026-08-08：地图落点特效先按施法者到落点方向解析方向分组起始帧，再创建特效节点；修复方向帧已设置但起始帧仍固定为 Up 的问题。
- 2026-08-08：投射物飞行期间按起点到实时目标的 RenderY 动态排序，避免移动中的技能穿过建筑/角色。
- 2026-08-08：怪物/NPC 名称改为按当前 BodyFrame 的真实图像顶部定位，避免固定标签高度造成大型对象重叠或小型 NPC 标签漂移。
- 2026-08-08：阴影按原版机制重新校准：普通玩家使用当前帧身体与装备组合的斜切压扁投影；怪物/NPC 优先使用当前帧 Shadow 通道，资源不可用时才用主体轮廓兜底；坐骑保留专用 Shadow 通道，物品保留资源 Shadow。轮廓阴影锚定到对象脚底 `(12,16)`，并跟随大型对象纵向偏移；DustDevil/Tornado/SabukGate 保持原版不画普通阴影。
- 2026-08-08：按当前 Godot 需求移除极暗档：`MinimumAmbient=100/255`；Night/Twilight 和 DayTime 经过底线后均不会低于该值，避免夜间整张地图不可见。
- 2026-08-08：特效透明审计发现 `ProgUse` 第 594 帧 DXT 近黑背景残留；`ZlReader` 现对所有特效缓存执行四角连通背景清理，2x 回归 `cornerPollution=0`。
- 2026-08-08：`MapTestScene --render-audit` 复测通过，实际检查 Guard、Hexa Holy Stone、玩家和 Gold；截图中对象阴影随主体轮廓变化，阴影与脚底基线保持一致。编译 0 errors，`git diff --check` 通过。
- 2026-08-08：2x 回归审计场景已与 `GameScene` 一样启用世界 `Scale=2`；2x 截图确认地图纹理、角色、标签、血条、阴影和像素过滤均按逻辑坐标放大，没有二次放大坐标。
- 2026-08-08：地图中层与前景拆为两组 `MapTerrainRow`，排序固定为 `99+CellY`（中层）→ `100+RenderY`（对象）→ `101+CellY`（前景），修复前景树冠/建筑无法遮挡角色的问题；该排序在世界 2x 下保持不变。
- 2026-08-08：2x `--action-audit` 通过，Walking、Running、Horse、攻击、施法、蓄力、受击、采集、钓鱼、驯服、隐身、推开和死亡等动作全部 PASS（27 条 PASS 记录）。
- 联机自动登录检查因当前沙箱禁止 socket，未把该权限错误误判为渲染错误。
- 桌面截图命令因当前容器没有可用 X11/Wayland display，无法读取实际像素；这属于运行环境限制，不是代码测试失败。

## 动作链路修复记录（2026-08-07）

- 移动回包不再丢弃 `Distance`：单格使用 Walking，多格/骑马使用 Running/HorseRunning，并保留六帧插值窗口。
- 选中远处怪物后会自动发起接近移动；进入一格内才发送普通攻击，避免“选中但永远不攻击”。
- 攻击发送时本地立即播动作，服务端回包再以职业、武器形状和 `MagicType` 校正；远程攻击回包也已接入。
- 修正帧序列循环取模、死亡/受击/推开等一次性动作的收尾，并恢复双持武器背层选择。
- 验证：`dotnet build GodotClient/ZirconClient.csproj --no-restore` 通过，0 errors；`--render-audit` 对象绘制通过。
- 交互补修：右键移动现在明确标记 Running，左键点击怪物身体或名称都会立即攻击/接近；怪物名称增加非空兜底，避免数据包名称为空时完全不显示。
- 移动再校正：玩家改为原版 `MovingOffSet` 语义，权威格立即到终点、画面按 Walking/Running 六帧时长从起点回拉，避免整数 `Round` 导致走路和跑步跳格。

## UI 迁移记录（2026-08-08）

- `NPCDialog` 已覆盖原版 `NPCDialogType` 的工艺分支：维修、精炼石、普通/大师精炼、精炼取回、碎片、饰品升级/升级/重置/合成、武器制作、戒指、伙伴管理和骰子/Yut；临时格统一通过 `CellLinkInfo` 提交，不会误移动背包物品。
- 补齐寄售搜索/分页、钓鱼/收竿、驯马、自动寻路、地下城查找、角色外观、队伍血条、行会成员权限、里程碑达成弹窗，并接入对应客户端/服务端包。
- 技能列表保留原版职业页签、技能学校翻页、内部滚动和快捷键绑定；快捷栏增加 24 槽分页与实时冷却覆盖。
- 开箱/礼包源物品在服务端返回前锁定，关闭窗口或完成确认时解除锁定。
- 串行构建与 headless 回归：GameScene、MapTest、UITest 均退出码 0；日志未发现脚本错误、资源解码异常或未处理异常。
# 贴图透明键补充

旧版客户端的 `ProgUse.Zl` 天气和特效帧使用黑色透明键。DXT 解码后，透明区域的 RGB 可能因压缩插值变成近黑色而不再是精确的 `(0, 0, 0)`；Godot 端在普通特效缓存中将 RGB 各通道不超过 32 的像素转为 Alpha 0，天气专用缓存放宽到 96，以清除天气帧压缩后残留的黑边。该规则只用于特效/天气缓存，地图、人物、装备等普通贴图仍保留合法的黑色像素。

天气粒子使用与旧版相同的逻辑坐标和原始帧尺寸，出生区域按屏幕尺寸除以 `WorldScale=2` 计算，并由 `GameScene` 的世界节点统一放大 2 倍；不能在 `MapWeatherLayer` 内再次把位置或纹理尺寸乘 2，否则会出现天气密度、速度和粒子大小翻倍。
