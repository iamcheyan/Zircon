const input=document.querySelector('[data-search]'), buttons=[...document.querySelectorAll('[data-filter]')], cards=[...document.querySelectorAll('[data-card]')];let filter='all';
function render(){const q=(input?.value||'').trim().toLowerCase();cards.forEach(c=>{const ok=filter==='all'||(c.dataset.tags||'').includes(filter);c.classList.toggle('hide',!ok||(q&&!c.innerText.toLowerCase().includes(q)))});}buttons.forEach(b=>b.addEventListener('click',()=>{filter=b.dataset.filter;buttons.forEach(x=>x.classList.toggle('active',x===b));render()}));input?.addEventListener('input',render);
