# 内容与数据库工具

这里负责地图目录、装备/技能/怪物/NPC、DAT 解码、商店和百科数据聚合。

默认输出主要位于 `docs/` 或 `/tmp/`，具体以各脚本的 `--help` 和文件头说明为准。

```bash
python3 Tools/content/build_content_catalog.py --help
python3 Tools/content/parse_mir3_magic_exp.py --help
```
